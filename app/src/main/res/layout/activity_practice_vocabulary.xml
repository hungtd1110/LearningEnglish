<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="#ffffff"
    android:focusableInTouchMode="true"
    android:focusable="true"
    tools:context="com.example.admin.learningenglish.vocabulary.activities.PracticeVocabularyActivity">

    <Button
        android:id="@+id/btnCheck"
        android:text="Check"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:layout_marginTop="10dp"
        android:background="@color/vocaularyTrue"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/edtAnswer"
        android:textSize="20sp"
        android:hint="Nhập câu trả lời"
        android:singleLine="true"
        android:layout_marginTop="10dp"
        android:layout_above="@+id/btnCheck"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ScrollView
        android:layout_above="@+id/edtAnswer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:layout_gravity="center"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/txtQuestion"
                android:text="Câu hỏi"
                android:textSize="20sp"
                android:textColor="#000000"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/txtAnswer"
                android:visibility="gone"
                android:layout_marginTop="5dp"
                android:text="Đáp án"
                android:textSize="20sp"
                android:textColor="@color/vocaularyTrue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </ScrollView>
</RelativeLayout>
